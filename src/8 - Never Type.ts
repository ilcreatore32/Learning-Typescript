/* Learning unknown type
   tipado desconocido
*/

import { userInput } from "./7 - Unknown Type";

//const number:number = userInput;

if (typeof userInput === "number") {
  const number: number = userInput;
}
